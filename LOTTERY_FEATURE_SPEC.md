# 抽奖功能开发需求文档

> 本文档说明本轮迭代在原问卷系统基础上新增/变更的"抽奖"相关功能，供开发人员参考实现细节、接口约定与验收标准。文中"原版本"指本轮迭代开始前的系统状态。

## 一、背景与目标

问卷系统原本只支持三种"提交成功后"的行为：显示完成页 / 跳转指定网页。本次迭代新增第三种模式——**进入抽奖页面**，让问卷方可以在玩家提交问卷后立即触发一次抽奖，配置奖品、页面文案、外观、多语言、领奖流程、时限作废与后台数据统计的完整链路。

---

## 二、总体功能地图

```
问卷设置（提交成功后 = 进入抽奖页面）
 ├─ 奖品池（九宫格，最多 8 个奖品位）
 │   └─ 每个奖品：名称 / 图片 / 类型（虚拟奖品｜实体奖品｜兑换码）/ 数量或兑换码
 ├─ 抽奖规则（中奖概率）
 ├─ 背景设置（抽奖页背景图）
 ├─ 领奖设置（按奖品类型分组配置）
 │   ├─ 用户需要填写的领奖信息（固定字段 + 自定义字段）
 │   ├─ 是否生成随机身份码
 │   └─ 领奖时间限制（5 分钟 ~ 48 小时，默认 10 分钟）
 └─ 页面文案设置（抽奖页 / 中奖页 / 未中奖页 / 抽奖完成页，标题+正文+按钮文字+内联链接）

外观模块
 └─ 抽奖页预览（应用全局主题，含抽奖页/中奖页/填写领奖信息/未中奖页/抽奖完成页五个预览 tab）

多语言模块
 └─ 抽奖流程页面文案 + 领奖字段标签 + 奖品名称 的多语言对照、翻译、导出导入

玩家端（/s/[slug]）
 ├─ 提交问卷后自动进入抽奖流程（转盘动画 → 中奖/未中奖结果 → 领奖信息填写 → 完成）
 └─ 领奖时限倒计时与超时作废提示

明细模块（原"答卷明细"重命名为"明细"，新增二级 tab）
 ├─ 答卷明细（原有功能不变）
 └─ 抽奖明细（新增）：参与/中奖/领取统计、奖品领取进度、中奖人明细表格
```

---

## 三、详细变更说明

### 3.1 问卷设置页（`/survey/[id]/settings`）

#### 3.1.1 提交成功后 —— 新增"进入抽奖页面"选项
- 原有"显示完成页 / 跳转指定网页"两个单选，新增第三个：**进入抽奖页面**。
- **重要变更**：选择"进入抽奖页面"后，下方原本会显示的"问卷结束页"和"按答案跳转"两个配置区块**自动隐藏**（因为进入抽奖流程后这两个配置不再需要，避免运营人员误配置）。切换回其他两种模式时，这两个区块恢复正常显示。

#### 3.1.2 奖品池
- 3x3 宫格布局（中心格固定为"奖品池"标识，实际可配置 8 个奖品位）。
- 点击格子打开"奖品配置"弹窗，可新增/编辑/删除该格奖品。
- 每个奖品字段：
  - 奖品名称（必填）
  - 奖品图片（选填，≤5MB，JPG/PNG/WEBP/GIF）
  - **奖品类型**（三选一，互斥）：
    - **虚拟奖品**：填写"奖品数量"
    - **实体奖品**：填写"奖品数量"
    - **兑换码**：上传 txt/Excel 文件或粘贴文本导入兑换码列表（每行一个），数量由兑换码条数自动决定
- 格子显示：奖品图片、名称、剩余库存；空格子悬停显示"+ 添加奖品"。

> **变更说明**：奖品类型原本是"虚拟奖品/实体奖品"两级分类（虚拟奖品下还要再选"兑换码/自定义领奖"），本轮已**拉平为三个并列类型**，编辑体验更直接。

#### 3.1.3 抽奖规则
- "中奖概率"输入框（0-100%）。中奖后，系统按各奖品当前剩余库存加权随机抽取具体奖品（库存越多概率越高）。
- 系统默认每个用户身份（JoyaMaker ID / LINE ID / 客户端 IP，优先级依次）只能抽奖一次，不可重复中奖，无需额外配置。

#### 3.1.4 背景设置
- 上传抽奖页面背景图（JPG/PNG，≤5MB）。

#### 3.1.5 领奖设置（本轮重点变更区域）

**变更历史**：
1. 最初版本：每个奖品单独配置"用户需要填写的领奖信息""是否生成身份码""领奖说明"。
2. 中间版本：改为按**奖品类型**（而非单个奖品）全局配置，去掉了单奖品的"领奖说明"字段，同时去掉了整个模块级的"发奖联系人姓名/电话/邮箱"和"领奖说明（选填）"字段——**这两处联系人信息字段已彻底移除**，不再作为抽奖配置的一部分。
3. 当前最终版本（本轮定稿）：

- 领奖设置卡片只显示**奖品池中实际用到的奖品类型**对应的配置块（例如奖品池里只配置了虚拟奖品和兑换码，这里就只出现这两个类型的配置块；配置了三种类型就出现三个块）。
- 若奖品池尚未配置任何奖品，显示空状态提示"请先在奖品池中添加奖品，再回来配置对应类型需要用户填写的领奖信息"。
- 每个类型配置块包含：
  1. **请选择用户填写的信息**（多选下拉）：
     - 固定字段（可勾选）：姓名、电话、地址、邮箱、Discord ID、JoyMaker ID
     - 自定义字段：可自行添加任意名称的字段（如"游戏角色名"）
     - 实体奖品类型首次选中时默认预勾选"姓名、电话、地址"三个固定字段
  2. **生成随机身份码**（开关）：开启后，该类型奖品中奖用户会额外获得一个格式为 `ID-XXXXXXXX` 的身份码，用于客服核验，也会在抽奖明细中作为"中奖人唯一标识"之一展示。
  3. **领奖时间限制**（下拉框，**本轮新增**）：中奖用户须在此时限内完成领奖信息填写，超时后奖品自动作废并释放库存。可选项：
     - 5 分钟 / 10 分钟 / 30 分钟 / 1 小时 / 3 小时 / 5 小时 / 12 小时 / 24 小时 / 48 小时
     - **默认值：10 分钟**
     - 兑换码类型即使没有配置任何"用户需要填写的信息"，该下拉框仍然存在（因为时限设置对所有类型都生效，只是没有填写字段的奖品会立即视为"已领取"，不会进入"待领取"倒计时状态，详见 3.4 节）。

#### 3.1.6 页面文案设置
- 4 个 tab：抽奖页面 / 中奖页面 / 未中奖页面 / 抽奖完成页面。
- 各 tab 可配置标题、正文、按钮文字；中奖页面/未中奖页面还支持在正文中插入内联链接（点击"+ 在正文光标处插入链接"，可插入多个）。
- 支持按"来源语言"（问卷默认语言）编辑，最终会通过"多语言"模块提供其他语言的翻译对照（见 3.3 节）。

---

### 3.2 外观模块（`/survey/[id]/appearance`）—— 新增"抽奖页"预览

- 原有预览状态切换器只有"填写页 / 问卷结束页 / 停止收集页"三种，本轮新增第四种：**抽奖页**。
- **仅当该问卷"提交成功后"设置为"进入抽奖页面"时，才会出现"抽奖页"这个预览入口**，未开启抽奖的问卷不显示，避免空数据困惑。
- 点击"抽奖页"后展示五个子 tab，均实时应用当前配置的全局主题样式（主色调、圆角、字体大小、按钮样式等）：
  1. **抽奖页面**：背景图 + 标题/正文 + 奖品九宫格（含奖品图片与名称）+ 按钮文字
  2. **中奖页面**：背景图 + 标题/正文（含内联链接）+ 示例奖品展示 + 按钮文字
  3. **填写领奖信息**：中奖用户需要填写的字段表单预览（示例字段取自奖品池中已配置的领奖信息设置，若都未配置则展示默认的姓名/电话/地址）
  4. **未中奖页面**：背景图 + 标题/正文（含内联链接）+ 按钮文字
  5. **抽奖完成页面**：标题/正文 + 按钮文字（用户提交领奖信息后展示的内容）

---

### 3.3 多语言模块（`/survey/[id]/languages`）—— 新增抽奖相关翻译对照

原多语言页面只支持问卷题目、问卷结束页、停止收集页的翻译对照，本轮新增：

1. **"抽奖流程页面"翻译区块**（仅问卷开启抽奖时显示）：用 tab 切换抽奖页面/中奖页面/未中奖页面/抽奖完成页面，每个 tab 展示"原文只读预览 + 目标语言可编辑表单"，可翻译标题、正文、按钮文字，以及中奖页/未中奖页正文中的内联链接文字；预览区域会显示真实配置的背景图，方便对照真实视觉效果校对文案。
2. **"领奖信息填写字段"翻译列表**：固定字段（姓名/电话/地址/邮箱/Discord ID/JoyMaker ID）和各类型下的自定义字段标签均可翻译，避免非中文用户看到中文字段名。
3. **"奖品池 / 奖品名称"翻译区块**：奖品池中每个奖品的名称也可单独翻译。

以上新增的翻译字段均自动纳入现有的翻译进度统计、CSV/Excel/飞书表格导出导入、AI 翻译辅助功能，无需额外操作。

---

### 3.4 玩家端抽奖流程（`/s/[slug]`）

#### 3.4.1 触发条件
问卷提交成功后，若该问卷"提交成功后"设置为"进入抽奖页面"，自动跳转进入抽奖流程（转盘动画 → 结果展示），不再显示原来的完成页或跳转逻辑。

#### 3.4.2 抽奖与防重复
- 系统按用户身份标识（JoyaMaker ID / LINE ID / 客户端 IP，取优先可用的一个）判断是否已经抽过奖；已抽过则直接展示上次抽奖结果，不会重复抽奖。
- 新用户抽奖：按配置的中奖概率判定是否中奖，中奖后按各奖品剩余库存加权随机选择具体奖品，同时扣减该奖品库存（虚拟/实体奖品扣减数量，兑换码类型弹出一个兑换码并从池中移除）——**扣减动作即"锁定库存"**，避免同一份库存被其他人重复抽中。

#### 3.4.3 中奖/未中奖展示
- 中奖：展示中奖页面文案、中奖奖品图片与名称、兑换码（如为兑换码类型）、随机身份码（如已开启）。
- 未中奖：展示未中奖页面文案。
- 若该奖品类型配置了"需要用户填写的信息"字段，中奖用户会看到"请填写领奖信息"表单；填完并提交后展示"抽奖完成页面"文案。

#### 3.4.4 领奖时限与超时作废（本轮新增核心逻辑）

- 中奖后，若该奖品类型**没有**配置任何领奖字段，视为无需额外操作，**立即标记为"已领取"**，不进入倒计时。
- 中奖后，若该奖品类型**配置了**领奖字段，抽奖记录状态变为"**待领取**"，并根据该类型设置的"领奖时间限制"计算出一个截止时间（中奖时间 + 时限分钟数）。
- **用户在时限内提交领奖表单** → 状态变为"**已领取**"，记录提交时间，库存不退回（因为已经在抽奖时正式锁定并最终确认发放）。
- **用户超过时限仍未提交领奖表单** → 该记录状态自动变为"**已过期**"：
  - 若为虚拟/实体奖品：库存数量 +1，释放回奖品池，可被其他人再次抽中
  - 若为兑换码：之前发出的那个兑换码重新放回兑换码池，可被再次抽中
  - 玩家此时若仍停留在领奖表单页面或重新打开链接，会看到"已超过领奖时限，本次中奖已作废"的提示，无法再提交领奖表单
- 该结算逻辑（检查所有"待领取"记录是否超时并释放库存）会在以下三个入口被自动触发，不需要人工干预：
  1. 玩家端每次进入抽奖流程时（`runLottery`）
  2. 问卷设置页加载时
  3. 抽奖明细页面加载时

---

### 3.5 明细模块（`/survey/[id]/responses`）—— 原"答卷明细"拆分重构

#### 3.5.1 导航变更
- 顶部导航栏原按钮文案"答卷明细"改为"**明细**"，路由不变（仍是 `/survey/[id]/responses`）。

#### 3.5.2 二级 Tab
- 进入页面后，顶部新增二级 tab：**答卷明细** / **抽奖明细**。
- "抽奖明细" tab **仅当该问卷开启了抽奖功能时才显示**；未开启抽奖的问卷只有"答卷明细"一个视图（与原版本完全一致，无任何变化）。

#### 3.5.3 答卷明细（原有功能，无变化）
字段展示、显示字段自定义、搜索字段自定义、语言/状态筛选、分页、导出（Excel/CSV/飞书多维表格）、答卷有效规则配置等功能均保持原样，未做任何改动。

#### 3.5.4 抽奖明细（全新功能）

**顶部统计**（三项并列卡片）：
- 参与抽奖人数（所有抽奖记录数，含未中奖）
- 中奖人数（`prizeId` 不为空的记录数）
- 领取奖品数（状态为"已领取"的记录数）

**奖品领取情况**（每个奖品一张卡片，横向平铺）：
- 奖品图片、名称、类型
- "剩余 X / 共 Y"（Y = 当前剩余库存 + 所有未过期的中奖记录数，即总投放量）
- 领取进度条（已领取数 / 总投放量）
- 库存已耗尽的奖品标注"已抽完"角标
- 从未被抽中的奖品，进度条自然显示为空——不额外用文字总结"还有哪些奖品没被抽中"，由卡片状态本身直观表达

**中奖明细表格**（仅展示中奖记录，未中奖不列入）：
- 默认展示列：中奖编号、答卷编号、奖品类型、奖品名称、领奖状态、中奖时间、领奖时间，以及该问卷所有奖品类型下配置过的领奖字段（如姓名、电话等，**用户填写的实际内容**，本次修复要点）
- 可选展示列（默认不勾选，可在"显示字段"里手动开启）：身份码
- **中奖编号**：系统生成的记录唯一标识（`draw-时间戳-随机串`），运营人员可用其在后台唯一定位一条抽奖记录，与"身份码"（面向用户展示、需该类型开启身份码功能才有值）是两个不同概念
- 领奖状态：待领取 / 已领取 / 已过期（对应不同背景色标识）
- 支持"显示字段"多选、"搜索字段"多选（可搜答卷编号/身份码/奖品名称/各领奖字段内容）、按领奖状态筛选、分页，交互方式与"答卷明细"表格完全一致

---

## 四、数据结构变更（供后端/前端联调参考）

### 4.1 `LotteryPrize`（单个奖品）
```ts
{
  id: string;
  slot: number;          // 九宫格位置 0-7
  name: string;
  image: string;         // base64 或 URL
  type: "virtual" | "physical" | "code";
  stock: number;         // 虚拟/实体奖品剩余数量
  codes?: string[];      // 兑换码类型的剩余兑换码列表
}
```
> 变更：原本挂在此对象上的 `claimFields`（领奖字段）、`identityCodeEnabled`（身份码开关）、`claimInstructions`（领奖说明）三个字段已移除，改为下方 `claimSettingsByType` 按类型统一配置。

### 4.2 `LotteryClaimSettingsByType`（按类型的领奖配置，新结构）
```ts
type LotteryClaimTypeSettings = {
  claimFields: { key: string; label: string }[];
  identityCodeEnabled: boolean;
  claimWindowMinutes: number;   // 新增：领奖时限（分钟），默认 10
};
type LotteryClaimSettingsByType = Record<"virtual" | "physical" | "code", LotteryClaimTypeSettings>;
```

### 4.3 `LotteryDrawRecord`（单条抽奖记录，新增字段）
```ts
{
  id: string;                 // 新增：中奖编号，唯一标识
  identityKey: string;        // 用户身份标识
  responseId: string;         // 关联答卷 ID
  prizeId: string | null;     // 中奖奖品 ID，未中奖为 null
  dispensedCode?: string;     // 兑换码类型发出的码
  identityCode?: string;      // 随机身份码
  claim?: Record<string, string>;  // 用户填写的领奖信息
  drawnAt: string;             // 中奖时间（ISO）
  claimStatus: "none" | "pending" | "claimed" | "expired";  // 新增：领奖状态
  claimDeadline?: string;      // 新增：领奖截止时间（ISO）
  claimedAt?: string;          // 新增：用户提交领奖信息的时间（ISO）
}
```

### 4.4 库存结算函数
新增纯函数 `settleExpiredLotteryDraws(config, draws, now)`：遍历所有"待领取"记录，找出已超时的，将其状态置为"已过期"并释放对应奖品库存/兑换码，返回更新后的配置与记录列表。该函数在设置页、明细页、玩家端三处调用，均为幂等操作（重复调用不会重复释放库存）。

---

## 五、涉及文件清单（供开发人员定位代码）

| 文件 | 变更内容 |
|---|---|
| `lib/survey-lottery.ts` | 核心数据类型与抽奖逻辑（奖品类型、领奖配置、抽奖记录、库存结算函数等） |
| `lib/survey-publication.ts` | 抽奖配置的默认值兼容与旧数据迁移逻辑 |
| `components/LotteryGrid.tsx` | 九宫格奖品池展示组件 |
| `components/LotteryPrizeEditor.tsx` | 单个奖品编辑弹窗 |
| `components/LotteryClaimFieldsSelect.tsx` | 领奖字段多选下拉组件（固定字段+自定义字段） |
| `app/survey/[id]/settings/page.tsx` | 设置页：提交成功后选项、奖品池、抽奖规则、背景、领奖设置、页面文案 |
| `app/survey/[id]/appearance/page.tsx` | 外观模块：抽奖页预览五个子 tab |
| `app/survey/[id]/languages/page.tsx` | 多语言模块：抽奖流程页面/领奖字段/奖品名称翻译对照 |
| `app/survey/[id]/responses/page.tsx` | 明细模块：二级 tab、抽奖明细统计与表格 |
| `app/survey/[id]/survey-nav.tsx` | 导航栏文案"答卷明细"→"明细" |
| `app/s/[slug]/page.tsx` | 玩家端：抽奖流程、领奖表单提交、时限结算逻辑 |
| `app/lottery.css` | 抽奖相关全部样式 |
| `app/globals.css` | 少量复用样式补充（进度条、状态标签等） |

---

## 六、验收测试要点

1. **设置页**：
   - 提交成功后切换到"进入抽奖页面"，确认"问卷结束页"与"按答案跳转"区块隐藏；切回其他模式恢复正常。
   - 奖品池分别添加虚拟奖品、实体奖品、兑换码三种类型，确认编辑弹窗字段符合各类型预期。
   - 领奖设置只展示奖品池中实际存在的类型对应配置块；调整每个类型的字段选择、身份码开关、领奖时限并保存，刷新页面确认保留。
2. **外观模块**：确认"抽奖页"预览入口仅在开启抽奖的问卷上出现；五个子 tab 内容与设置页配置一致，且随主题色/圆角等全局样式联动变化。
3. **多语言模块**：切换目标语言，确认抽奖四个流程页面、领奖字段、奖品名称均可填写译文并正确回填预览；导出的 CSV/Excel 包含以上新增字段。
4. **玩家端**：
   - 走一遍完整抽奖流程（中奖+需要填写信息 / 中奖+无需填写信息 / 未中奖三种分支）。
   - 中奖后不提交领奖表单，等待超过配置的时限后重新进入，确认记录状态变为"已过期"，对应奖品库存/兑换码已释放，页面提示领奖已作废。
   - 正常提交领奖表单，确认状态变为"已领取"，领奖时间正确记录。
5. **明细模块**：
   - 未开启抽奖的问卷，确认"明细"页面只有"答卷明细"，无"抽奖明细"选项。
   - 开启抽奖后，确认"抽奖明细"顶部统计数字、奖品进度卡片、中奖表格默认展示的领奖字段列（用户实际填写内容）均正确显示；"显示字段"/"搜索字段"/筛选/分页均可正常使用。
